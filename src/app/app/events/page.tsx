import { Card } from "../../../ui/primitives";

export default function EventsPage() {
  return (
    <Card>
      <h2 className="rf-sr-only">Events</h2>
      <p className="rf-status rf-status-muted">Events module placeholder.</p>
    </Card>
  );
}
